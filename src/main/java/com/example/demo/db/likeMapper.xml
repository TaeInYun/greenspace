<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="like">

	<select id="etsLike">
		select ets_no from like where member_no=#{no}
	</select>

	<select id="ecoLike">
		select eco_no from like where member_no=#{no}
	</select>
	
	<select id="cerLike">
		select cerLike from like where member_no=#{no}
	</select>
	

</mapper>