<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
  <select id="findAll" resultType="memberVO">
    select id, pwd,name,nickname,email,phone from member 
	where no=1
  </select>
   
   
   
   <update id="updatenickname" parameterType="java.lang.String">
   	  update member set id=#{id} where no=#{no}
   </update>
   
   <select id="checknickname" resultType="java.lang.String">
   		select nickname from member where id=#{no}
   </select>
   
   
   
   
</mapper>